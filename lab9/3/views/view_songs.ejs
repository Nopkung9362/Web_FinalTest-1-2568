<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=\, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        table {
            border-collapse: collapse;
        }

        body {
            margin: 20px;
            width: 850px;
        }

        th,
        td {
            border: 1px solid;
            padding: 10px;

        }

        th {
            background-color: lightblue;
        }

        td {
            background-color: lightyellow;
        }
    </style>
</head>

<body>
    <h2>67070125 Lab 9/3 : Template Engine and Work with Database</h2>
    <table class="table table-striped">
        <thead>
            <tr>
                <th class="info">Songs</th>
                <th class="info">Artist</th>
                <th class="info">Released</th>
                <th class="info">Type</th>
            </tr>
        </thead>
        <tbody>
            <!-- สร้างตารางสำหรับแสดงข้อมูล -->

            <% data.forEach((item)=>{ %>
                <tr>
                    <td>
                        <%=item.song%>
                    </td>
                    <td>
                        <%=item.artist%>
                    </td>
                    <td>
                        <%=item.year%>
                    </td>
                    <td>
                        <%=item.type%>
                    </td>
                </tr>


                <% }); %>
        </tbody>
    </table>
</body>

</html>