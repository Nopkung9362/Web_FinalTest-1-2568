<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="styles.css">
</head>

<body>
    <h1>Product List</h1>
    <hr>

    <div class="grid-container">
        <% data.forEach((item)=>{ %>
            <div class="card">
                <img src="<%= item.image_url %>" style="width: 250px; height: 200px;"> <br>
                <div class="detail">
                    <p>
                        <%= item.name %>
                    </p> <br>
                    <p>
                        <%= item.description %>
                    </p> <br>
                    <a href="/detail/<%=item.product_id%>">Details</a>
                </div>
            </div>
            <% }); %>
    </div>
</body>

</html>